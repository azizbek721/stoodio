import React from 'react';

const Header = () => {
    return (
        <div className="container mx-auto">
            Header component for Team page
        </div>
    );
};

export default Header;
